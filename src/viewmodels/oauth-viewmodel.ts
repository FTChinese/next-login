import { IAuthorizeRequest } from "../models/ftc-oauth";
import { IFormState } from "./validator";

class OAuthViewModel {
    validate(query: IAuthorizeRequest): IFormState<IAuthorizeRequest> {
        
    }
}
